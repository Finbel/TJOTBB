package Main;

/**
 * Class Space - represents an open square (space).
 */
public class Space extends Square
{
    /**
     * Initially, it has no neighbours or items.
     * 
     * @param x The x-coordinate.
     * @param y The y-coordinate.
     * 
     */
    public Space(int x, int y) {
    	super(x, y);
    }
}